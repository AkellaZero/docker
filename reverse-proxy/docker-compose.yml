services:
  reverse-proxy:
    container_name: reverse-proxy
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - storage:/etc/nginx/conf.d
    restart: unless-stopped

volumes:
  storage:
